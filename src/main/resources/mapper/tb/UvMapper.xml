<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.kalvin.kvf.modules.tb.mapper.UvMapper">

    <!-- 通用查询映射结果 -->
    <resultMap id="BaseResultMap" type="com.kalvin.kvf.modules.tb.entity.Uv">
        <id column="id" property="id" />
        <result column="user_id" property="userId" />
        <result column="article_id" property="articleId" />
        <result column="ip" property="ip" />
        <result column="status" property="status" />
        <result column="create_date" property="createDate" />
    </resultMap>

    <!-- 通用查询结果列 -->
    <sql id="Base_Column_List">
       id, user_id, article_id, ip, status, create_date
    </sql>

    <select id="selectUvList" resultType="com.kalvin.kvf.modules.tb.entity.Uv">
        select <include refid="Base_Column_List" /> from tb_uv
        <trim prefix="where" prefixOverrides="and">
            <if test="uv.userId != null">
                and user_id = #{uv.userId}
            </if>
            <if test="uv.articleId != null">
                and article_id = #{uv.articleId}
            </if>
            <if test="uv.ip != null and uv.ip != ''">
                and ip like concat(#{uv.ip}, "%")
            </if>
            <if test="uv.status != null">
                and status = #{uv.status}
            </if>
            <if test="uv.createDate != null">
                and create_date = #{uv.createDate}
            </if>
        </trim>
    </select>

</mapper>
